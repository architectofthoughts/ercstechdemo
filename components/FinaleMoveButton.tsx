import React from 'react';

// This component is no longer used in the new "Gesture Based" Finale interaction.
// Keeping file structure but empty to avoid build errors if referenced elsewhere (though refs are removed).
const FinaleMoveButton: React.FC<any> = () => {
  return null;
};

export default FinaleMoveButton;